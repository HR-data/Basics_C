// Ther are many short hand used 

#include <stdio.h>

int main()
	{
	 int i,j,k;
	 printf("Enter any two number \n");
	 scanf("%d %d %d",&i,&j,&k);
	 // i =i +3
	 // we will use shorthand
	 i+=3;
	 j-=2;
	 k*=4;
	 
	 printf("The values are %d %d %d \n", i ,j ,k );
	
	 return 0; 
	
	}
